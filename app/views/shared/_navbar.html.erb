<nav class="navbar navbar-expand-lg sticky-top navbar-light bg-light navbar-custom">
  <a class="navbar-brand" href=<%= root_path %>>
      <img src="/assets/logo.png"  height="35" alt="">
  </a>
  
    <!-- Botão Hamburguer para telas pequenas -->
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Alterna navegação">
        <span class="navbar-toggler-icon"></span>
    </button>
    
    
  
    <!-- Tudo dentro dessa div vai sumir -->
  <div class="collapse navbar-collapse" id="navbarTogglerDemo01">

    <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
      <!--<li class="nav-item">
        <a class="nav-link" href=></a>
      </li>-->
    </ul>
    <% if user_signed_in? %>
          <strong><%= current_user.email %></strong>
          <button class = 'btn btn-light'><i class = "fas fa-cog"></i></button>
          <%= button_to "Sair", destroy_user_session_path, method: :delete, :class => 'btn btn-danger'%>
    <% else %>
          <strong class = "mr-sm">Novo aqui?</strong>
          <%= button_to "Comece agora!", root_path, class: "btn btn-success ml-sm", method: :get  %>
    <% end %>
  </div>
</nav>